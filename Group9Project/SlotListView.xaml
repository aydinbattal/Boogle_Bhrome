<Page
    x:Class="Group9Project.SlotListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Group9Project"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:classes="using:Group9Project"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <ListView>
            <DataTemplate x:DataType="classes:SlotMachineHistoryTemplate">
                <StackPanel Orientation="Horizontal">
                    <Image Width="100" Source="{x:Bind Image.Source}"/>
                    <StackPanel>
                        <TextBlock Text="{x:Bind Year}"/>
                        <TextBlock Text="{x:Bind Description}"/>
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
        </ListView>
    </StackPanel>
</Page>
